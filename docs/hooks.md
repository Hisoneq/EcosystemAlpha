# Документация по хукам

## useDebounce

### Описание
Хук для отложенного обновления значения (debounce pattern). Используется для оптимизации производительности при частых изменениях значений, таких как ввод в поле поиска.

### Сигнатура
```typescript
useDebounce<T>(value: T, delay?: number): T
```

### Параметры
- `value: T` - Значение, которое нужно задержать
- `delay: number` (опционально) - Задержка в миллисекундах (по умолчанию 500ms)

### Возвращает
`T` - Отложенное (debounced) значение

### Как это работает

1. **Мгновенное обновление**: Локальное состояние обновляется сразу
2. **Задержанная фильтрация**: Фильтрация происходит только после паузы в вводе
3. **Автоочистка**: Таймер автоматически очищается при новых изменениях

### Схема работы
```
Пользователь печатает: "a" → "ab" → "abc"
                        ↓      ↓      ↓
Локальное значение:    "a"   "ab"   "abc"  (мгновенно)
                        ↓      ↓      ↓
Таймеры:               🕐     🕐     🕐     (сбрасываются)
                                     ↓
                                  (300ms)
                                     ↓
Debounced значение:              "abc"     (с задержкой)
```

### Примеры использования

#### Базовый пример
```typescript
import { useDebounce } from '../hooks/useDebounce';

const SearchComponent = () => {
  const [searchTerm, setSearchTerm] = useState('');
  const debouncedSearch = useDebounce(searchTerm, 300);

  useEffect(() => {
    // API запрос выполнится только через 300ms после прекращения ввода
    if (debouncedSearch) {
      fetchSearchResults(debouncedSearch);
    }
  }, [debouncedSearch]);

  return (
    <input 
      value={searchTerm}
      onChange={(e) => setSearchTerm(e.target.value)}
    />
  );
};
```

#### Использование в useProductsList
```typescript
export const useProductsList = () => {
  // Локальное состояние для мгновенного UI
  const [localSearchQuery, setLocalSearchQuery] = useState('');
  
  // Debounced значение для фильтрации
  const debouncedSearchQuery = useDebounce(localSearchQuery, 300);

  const filteredProducts = useMemo(() => {
    // Используем debounced значение для тяжелой фильтрации
    return products.filter(p => 
      p.title.toLowerCase().includes(debouncedSearchQuery.toLowerCase())
    );
  }, [products, debouncedSearchQuery]);

  return {
    searchQuery: localSearchQuery,        // Мгновенное для input
    isSearching: localSearchQuery !== debouncedSearchQuery, // Индикатор
    filteredProducts,                     // Результат с задержкой
  };
};
```

#### С разными задержками
```typescript
// Короткая задержка для UI элементов
const debouncedValue = useDebounce(value, 150);

// Средняя задержка для поиска
const debouncedSearch = useDebounce(searchTerm, 300);

// Длинная задержка для тяжелых операций
const debouncedComputation = useDebounce(data, 1000);
```

### Преимущества

1. **Производительность**
   - Уменьшает количество вызовов API
   - Снижает нагрузку на рендеринг
   - Экономит ресурсы сервера

2. **UX**
   - Мгновенный отклик в UI
   - Нет лагов при вводе
   - Индикатор загрузки во время debounce

3. **Простота использования**
   - Всего одна строка кода
   - TypeScript Generic для типобезопасности
   - Работает с любыми типами данных

### Когда использовать

✅ **Используйте для:**
- Полей поиска
- Автокомплит
- Фильтрации больших списков
- API запросов при вводе
- Отслеживания размера окна (resize)
- Обработки scroll событий

❌ **Не используйте для:**
- Кнопок (используйте throttle)
- Критичных действий (submit формы)
- Одиночных событий
- Когда нужен мгновенный результат

### Альтернативы

#### Throttle (не реализован)
```typescript
// Throttle выполняет функцию не чаще раза в N ms
// Debounce ждет паузы в N ms

// Debounce: ввод → пауза → действие
// Throttle: ввод → действие → пауза → действие
```

#### Lodash debounce
```typescript
import { debounce } from 'lodash';

// Наш хук проще и не требует дополнительных зависимостей
const debouncedFn = debounce(myFunction, 300);
```

### Производительность

**До useDebounce:**
```
Ввод "react" (5 символов) = 5 запросов к API
Время: ~500ms на запрос = 2.5 секунды ожидания
```

**После useDebounce (300ms):**
```
Ввод "react" (5 символов) = 1 запрос к API
Время: ~500ms на запрос = 0.8 секунды общее время
```

**Экономия:** 80% запросов, 68% времени

### Визуальный индикатор

В `ProductList` добавлен индикатор "Поиск..." который показывается когда:
```typescript
isSearching = localSearchQuery !== debouncedSearchQuery
```

Это даёт пользователю обратную связь, что система обрабатывает его ввод.

### Технические детали

#### Очистка таймеров
```typescript
useEffect(() => {
  const timer = setTimeout(() => {
    setDebouncedValue(value);
  }, delay);

  // Важно: очищаем предыдущий таймер
  return () => clearTimeout(timer);
}, [value, delay]);
```

#### Generic типы
```typescript
// Работает с любыми типами
const debouncedString = useDebounce<string>('text', 300);
const debouncedNumber = useDebounce<number>(42, 300);
const debouncedObject = useDebounce<User>(user, 300);
```

### Тестирование

```typescript
import { renderHook, waitFor } from '@testing-library/react';
import { useDebounce } from './useDebounce';

test('debounces value', async () => {
  const { result, rerender } = renderHook(
    ({ value }) => useDebounce(value, 300),
    { initialProps: { value: 'initial' } }
  );

  expect(result.current).toBe('initial');

  rerender({ value: 'updated' });
  expect(result.current).toBe('initial'); // Еще не обновилось

  await waitFor(() => {
    expect(result.current).toBe('updated'); // Обновилось через 300ms
  }, { timeout: 500 });
});
```

### Best Practices

1. **Выбор правильной задержки**
   ```typescript
   // 150-200ms - для мгновенного поиска
   // 300-400ms - стандартный поиск
   // 500-1000ms - для API с rate limiting
   ```

2. **Комбинация с useMemo**
   ```typescript
   const debouncedQuery = useDebounce(query, 300);
   
   const filtered = useMemo(() => {
     return heavyComputation(debouncedQuery);
   }, [debouncedQuery]);
   ```

3. **Индикаторы загрузки**
   ```typescript
   const isDebouncing = value !== debouncedValue;
   return isDebouncing ? <Spinner /> : <Results />;
   ```

### Интеграция в проекте

Хук используется в:
- ✅ `useProductsList` - для поиска продуктов
- 🔄 Готов к использованию в других местах

### Расширения

Можно добавить:
```typescript
// Debounce с leading edge (выполнить сразу + в конце)
// Cancellable debounce (возможность отмены)
// MaxWait (максимальное время ожидания)
```

