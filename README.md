# Ecosystem Alpha - Product Management SPA

Одностраничное приложение для управления списком продуктов с чистой модульной архитектурой.

## 🚀 Технологический стек

- **React 19** - UI библиотека
- **TypeScript** - строгая типизация
- **Redux Toolkit** - управление состоянием
- **React Router** - маршрутизация
- **React Hook Form** - работа с формами
- **CSS Modules** - изолированные стили
- **Vite** - быстрая сборка
- **DummyJSON API** - источник данных

## 📋 Функциональность

### ✅ Список продуктов (`/products`)
- Карточки продуктов с изображениями
- Лайки с визуальной индикацией
- Удаление продуктов
- Фильтр: Все / Избранное
- Поиск в реальном времени
- Пагинация
- Переход на детальную страницу

### ✅ Детальная страница (`/products/:id`)
- Полная информация о продукте
- Галерея изображений с навигацией
- Управление лайками и удалением
- Навигация обратно к списку

### ✅ Создание продукта (`/create-product`)
- Валидация всех полей
- Обязательные поля с требованиями
- Сохранение в Redux store
- Автоматический редирект

## 📂 Архитектура проекта

```
src/
├── components/              # UI компоненты (без логики)
│   ├── ErrorMessage/
│   │   ├── ErrorMessage.tsx
│   │   └── ErrorMessage.module.css
│   ├── FilterTabs/
│   │   ├── FilterTabs.tsx
│   │   └── FilterTabs.module.css
│   ├── Loading/
│   │   ├── Loading.tsx
│   │   └── Loading.module.css
│   ├── Pagination/
│   │   ├── Pagination.tsx
│   │   └── Pagination.module.css
│   ├── ProductCard/
│   │   ├── ProductCard.tsx
│   │   └── ProductCard.module.css
│   └── SearchBar/
│       ├── SearchBar.tsx
│       └── SearchBar.module.css
├── hooks/                   # Бизнес-логика
│   ├── useProductsList.ts  # Логика списка продуктов
│   ├── useProductDetail.ts # Логика детальной страницы
│   ├── useProductForm.ts   # Логика формы создания
│   └── useDebounce.ts      # Утилита для debounce
├── pages/                   # Презентационные компоненты
│   ├── CreateProduct/
│   │   ├── CreateProduct.tsx
│   │   └── CreateProduct.module.css
│   ├── ProductDetail/
│   │   ├── ProductDetail.tsx
│   │   └── ProductDetail.module.css
│   └── ProductList/
│       ├── ProductList.tsx
│       └── ProductList.module.css
├── services/                # API сервисы
│   └── api.ts
├── store/                   # Redux store
│   ├── hooks.ts            # Типизированные хуки
│   ├── productsSlice.ts    # Слайс продуктов
│   └── store.ts            # Конфигурация
├── styles/                  # Глобальные стили
│   ├── global.css          # Базовые стили
│   └── variables.css       # CSS переменные
├── types/                   # TypeScript типы
│   └── product.ts
├── App.tsx                  # Главный компонент
└── main.tsx                 # Точка входа
```

## 🎨 Архитектурные принципы

### Разделение ответственности

#### 1. **Components** - Только UI
```typescript
// ❌ Плохо - логика в компоненте
const ProductCard = () => {
  const dispatch = useDispatch();
  const [data, setData] = useState();
  // ... много логики
}

// ✅ Хорошо - только рендер
const ProductCard = ({ product, onLike, onDelete }) => {
  return <div>...</div>
}
```

#### 2. **Hooks** - Вся бизнес-логика
```typescript
// Хук инкапсулирует всю логику
export const useProductsList = () => {
  // State management
  // Effects
  // Computed values
  // Event handlers
  
  return { /* публичный API */ }
}
```

#### 3. **Pages** - Композиция компонентов
```typescript
// Страница только соединяет компоненты и хуки
export const ProductList = () => {
  const logic = useProductsList();
  
  return (
    <div>
      <SearchBar {...logic} />
      <FilterTabs {...logic} />
      <ProductGrid {...logic} />
    </div>
  );
};
```

### CSS Modules
- **Изоляция стилей** - нет конфликтов имен классов
- **Локальность** - стили применяются только к своему компоненту
- **Масштабируемость** - легко добавлять новые компоненты
- **Читаемость** - стили находятся рядом с компонентом

### Custom Hooks паттерн

**useProductsList.ts** - логика списка
- Загрузка данных
- Фильтрация и поиск
- Пагинация
- Обработчики событий

**useProductDetail.ts** - логика детальной страницы
- Загрузка конкретного продукта
- Управление изображениями
- Действия с продуктом

**useProductForm.ts** - логика формы
- Работа с react-hook-form
- Валидация
- Отправка данных

## 🛠️ Установка и запуск

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение: http://localhost:5173

### Сборка для продакшена
```bash
npm run build
```

### Предпросмотр сборки
```bash
npm run preview
```

## 🎯 Redux Store

### State структура
```typescript
{
  products: Product[]           // Все продукты
  likedProducts: number[]       // ID избранных
  loading: boolean              // Статус загрузки
  error: string | null          // Ошибки
  searchQuery: string           // Поисковый запрос
  filterFavorites: boolean      // Фильтр избранного
  currentPage: number           // Текущая страница
  itemsPerPage: number          // Элементов на странице
  initialized: boolean          // Флаг инициализации
}
```

### Actions
- `fetchProducts` - загрузка продуктов (async)
- `toggleLike(id)` - добавить/убрать лайк
- `deleteProduct(id)` - удалить продукт
- `addProduct(product)` - добавить новый продукт
- `setSearchQuery(query)` - установить поиск
- `setFilterFavorites(bool)` - фильтр избранного
- `setCurrentPage(page)` - изменить страницу

## 💡 Особенности реализации

### Оптимизация
- **useMemo** для фильтрации и сортировки
- **useRef** для предотвращения повторных запросов
- **useDebounce** для оптимизации поиска (300ms задержка)
- **CSS Modules** для изоляции стилей
- **Custom hooks** для переиспользования логики

### Валидация форм
- **React Hook Form** с встроенной валидацией
- Минимальные и максимальные длины
- Проверка типов данных
- Паттерны для URL изображений

### CSS Переменные
Все цвета, тени, радиусы вынесены в `variables.css`:
```css
--primary-color: #667eea;
--shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.1);
--radius-md: 8px;
--transition: all 0.3s ease;
```

## 🌟 Преимущества архитектуры

### Чистота кода
- **Презентационные компоненты** - только JSX
- **Хуки** - изолированная логика
- **Однонаправленный поток данных**

### Тестируемость
- Логика в хуках легко тестируется
- UI компоненты можно тестировать с моками
- Нет смешивания concerns

### Масштабируемость
- Легко добавлять новые страницы
- Переиспользование логики через хуки
- Модульная структура

### Поддерживаемость
- Каждый файл < 200 строк
- Понятная структура проекта
- TypeScript для безопасности типов

### Расширяемость
- Готовность к добавлению новых фич
- Переиспользуемые компоненты
- Централизованное управление состоянием

## 📝 Примеры использования

### Создание нового хука
```typescript
// hooks/useMyFeature.ts
export const useMyFeature = () => {
  const dispatch = useAppDispatch();
  const data = useAppSelector(state => state.myData);
  
  const handleAction = () => {
    // логика
  };
  
  return { data, handleAction };
};
```

### Создание нового компонента
```typescript
// components/MyComponent/MyComponent.tsx
interface Props {
  data: string;
  onAction: () => void;
}

export const MyComponent = ({ data, onAction }: Props) => {
  return <div onClick={onAction}>{data}</div>;
};
```

### Создание новой страницы
```typescript
// pages/MyPage/MyPage.tsx
export const MyPage = () => {
  const logic = useMyFeature();
  
  return (
    <div>
      <MyComponent {...logic} />
    </div>
  );
};
```

## 📦 Зависимости

### Production
- `react` - UI библиотека
- `react-dom` - React для DOM
- `react-router-dom` - маршрутизация
- `@reduxjs/toolkit` - Redux
- `react-redux` - React биндинги для Redux
- `react-hook-form` - работа с формами

### Development
- `typescript` - типизация
- `vite` - сборщик
- `eslint` - линтер

## 🚀 Готово к продакшену

- ✅ TypeScript без ошибок
- ✅ ESLint без предупреждений
- ✅ Модульные стили
- ✅ Разделение презентации и логики
- ✅ Оптимизированная структура
- ✅ Готово к масштабированию
